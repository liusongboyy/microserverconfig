server:
  port: ${PORT:8761}
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      defualtZone: http://localhost:${server.port}/eureka/
      waitTimeInMsWhenSyncEmpty: 0
